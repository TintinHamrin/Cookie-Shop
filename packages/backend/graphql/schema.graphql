type Product {
  _id: Int!
  name: String!
  img: String!
  description: String!
  price: Int!
}

type Query {
  products: [Product]
  hello: String!
}

# type Mutation {
#   answerQuestion(q: ID!, lat: Float!, lng: Float!): Answer
# }

schema {
  query: Query
}
